"use strict";

module.exports = function(friendingLibrary) {

  friendingLibrary.factory(
    "AvailableBooks", ["REST", {function(rest) {

      // internal implementation : hash ? by copyid ?

      var books;
    }}]
  );
};
