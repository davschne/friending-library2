-access tokens
  -store in Redis
  -set expiration date
  -access Redis in authentication middleware, pass uID in request body

-update or insert in Postgres? Is this behavior necessary? For which endpoints?


Schemas
-------

User
----
uID          : varchar PRIMARY KEY
displayName  : varchar

Friend
------
uID1 : varchar references User(uID)
uID2 : varchar references User(uID)

Book
----
ISBN          : integer PRIMARY KEY
title         : varchar
subtitle      : varchar
publisher     : varchar
publishedDate : integer
description   : varchar
pageCount     : integer
language      : varchar
imageLink     : varchar

Author
------
aID  : serial PRIMARY KEY
name : varchar

Wrote
-----
aID  : integer references Author(aID)
ISBN : integer references Book(ISBN)

Copy
----
cID  : integer PRIMARY KEY
ISBN : integer references Book(ISBN)
uID  : integer references User(uID) - owner

Borrowing
---------
uID          : integer references User(uID) - borrower
cID          : integer references Copy(cID)
checkoutDate :

BookRequest
-----------
uID  : integer references User(uID) - requester
cID  : integer references Copy(cID)
date :

Category
--------

FriendRequest
-------------
requester : integer references User(uID)
recipient : integer references User(uID)
